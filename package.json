{
  "name": "@here.build/commons",
  "version": "0.0.0",
  "private": true,
  "description": "Shared floor of here.build and inhuman.tools: tsconfig bases, eslint configs, collections, and small infrastructure primitives",
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.9.0"
  },
  "packageManager": "pnpm@10.3.0",
  "engines": {
    "node": ">=22"
  }
}
